import type { GraphDimensions, XYPair } from '../../types';
export declare function calculateViewportCenter(dimensions: GraphDimensions, translation: XYPair, scale: number): {
    x: number;
    y: number;
};
